{
  // ===== INFORMASI PROJECT =====
  "name": "uts-pemweb-123140184",  // Nama project
  "private": true,                 // Project ini private (tidak dipublish ke npm)
  "version": "0.0.0",              // Versi project
  "type": "module",                // Kita pakai ES Modules (import/export modern)
  
  // ===== SCRIPTS/PERINTAH =====
  // Perintah yang bisa kita jalankan dengan npm run <nama-script>
  "scripts": {
    "dev": "vite",              // npm run dev: jalankan development server
    "build": "vite build",      // npm run build: build project untuk production
    "lint": "eslint .",         // npm run lint: check kode dengan ESLint
    "preview": "vite preview"   // npm run preview: preview hasil build
  },
  
  // ===== DEPENDENCIES =====
  // Package yang DIPERLUKAN untuk aplikasi berjalan (production)
  "dependencies": {
    "@tailwindcss/vite": "^4.1.16",  // Plugin Tailwind untuk Vite
    "react": "^19.1.1",               // React library
    "react-dom": "^19.1.1"            // React DOM untuk render ke browser
  },
  
  // ===== DEV DEPENDENCIES =====
  // Package yang hanya diperlukan saat development (tidak untuk production)
  "devDependencies": {
    "@eslint/js": "^9.36.0",                    // ESLint config untuk JavaScript
    "@types/react": "^19.1.16",                 // TypeScript types untuk React
    "@types/react-dom": "^19.1.9",              // TypeScript types untuk React DOM
    "@vitejs/plugin-react": "^5.0.4",           // Plugin React untuk Vite
    "autoprefixer": "^10.4.21",                 // Otomatis tambah vendor prefix di CSS
    "eslint": "^9.36.0",                        // ESLint untuk code quality
    "eslint-plugin-react-hooks": "^5.2.0",      // ESLint rules untuk React Hooks
    "eslint-plugin-react-refresh": "^0.4.22",   // ESLint rules untuk React Fast Refresh
    "globals": "^16.4.0",                       // Definisi global variables
    "postcss": "^8.5.6",                        // Tool untuk transform CSS
    "tailwindcss": "3.4",                       // Tailwind CSS framework
    "vite": "^7.1.7"                            // Vite build tool
  }
}
